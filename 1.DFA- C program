#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	int i;
	printf("enter a string: ");
	scanf("%s",str);
	int length = strlen(str);
	if(str[0] == 'a' && str[length-1] == 'a')
	{
		printf("accepted");
	}
	else
	{
		printf("rejected");
	}
	return 0;
}
